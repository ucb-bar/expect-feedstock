# Builds now run on GitHub Actions (.github/workflows/conda-build.yml).
# Keep a no-op Azure pipeline so an existing Azure definition does not fail.
jobs:
  - job: skip
    pool:
      vmImage: ubuntu-latest
    steps:
      - script: echo "Skipping Azure Pipelines; builds run on GitHub Actions."
        displayName: Skip Azure
